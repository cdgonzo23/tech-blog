<div class="blogpost">
    {{#each blogposts as |blogpost|}}
    {{> blogpost-details}}
    {{/each}}
</div>

<div class="new-post">
    <a href="/api/userblogpost">
        <button class="btn btn-primary">+ New Post</button>
    </a>
</div>
